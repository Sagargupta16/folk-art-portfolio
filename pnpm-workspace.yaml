packages:
  - "."

overrides:
  "postcss@<8.5.23": ">=8.5.23"
  # nanoid <3.3.18 (GHSA-2v37-7h3g-55p8) reaches us through postcss; cap at <4
  # because nanoid 4+ is ESM-only and postcss requires the CJS 3.x line.
  "nanoid@<3.3.18": ">=3.3.18 <4"
  esbuild: ">=0.28.1"
  # Keep Next's optional image optimizer and our R2 pipeline on the same
  # patched native parser, including the GHSA-rgj7-g3m4-5g8c fixes.
  sharp: "^0.35.4"
